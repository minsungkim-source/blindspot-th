# CONTRIBUTING

공개 저장소입니다. 이슈와 PR을 환영합니다.

## 시작하기

`README.md`의 개발 절차를 먼저 보세요. 할 일은 `docs/BACKLOG.md`에 순서대로 있습니다.
작업 규칙은 `CLAUDE.md`에 있고, 사람에게도 그대로 적용됩니다.

## PR 전에

```bash
npm run typecheck
cd etl && python -m pytest        # 테스트가 생긴 뒤
```

## 이 저장소에 넣으면 안 되는 것

공개 저장소이므로 아래는 예외 없이 거절됩니다.

- **내부·비공개 데이터** — TrueMoney 에이전트 위치, 거래 데이터, 고객 데이터,
  내부 KPI, 확장 계획. 이 저장소는 공개 출처 데이터만 다룹니다.
- **World Bank Findex 마이크로데이터** — 라이선스상 재배포 불가.
  지표 API의 집계값만 씁니다.
- **자격증명** — API 키, 토큰, 쿠키, 세션. v1의 모든 소스는 인증이 필요 없습니다.
  인증이 필요한 소스를 추가하려면 먼저 이슈로 논의해 주세요.
- **대용량 원본 파일** — `data/raw/`는 gitignore 되어 있습니다.

## 데이터 소스를 추가할 때

`DATA_SOURCES.md`에 다음을 반드시 함께 적습니다.

1. 정확한 URL과 실제 해상도 (주 단위인가, 권역 단위인가)
2. 갱신 주기와 확인 시점의 최신 데이터
3. **라이선스와 재배포 조건**
4. 신뢰등급 (A/B/C)과 그 근거

해상도가 주 단위보다 낮은 소스는 지도의 색 채널을 쓸 수 없습니다.
추정으로 내려 쓰고 화면에 `추정` 배지를 답니다 — `METHODOLOGY.md` §5 참조.

## 지수 산식을 바꿀 때

`etl/transform/figi.py`와 `src/lib/score.ts`가 같은 로직을 구현합니다.
**한쪽만 고친 PR은 머지하지 않습니다.** `METHODOLOGY.md`도 같이 갱신해 주세요 —
문서가 산식의 단일 출처입니다.
