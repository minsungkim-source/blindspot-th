# Blindspot TH — DESIGN.md

> [brands-design-md](https://github.com/ricocc/brands-design-md)의 DESIGN.md 형식을 따른다.
> 사람과 AI 코딩 도구가 같이 읽는 이식 가능한 디자인 브리프.

---

## 1. 전체 방향

**분석 도구이지 마케팅 페이지가 아니다.** 화면을 열면 지도와 숫자가 먼저 오고,
장식은 뒤로 물러난다. Linear·Vercel 계열의 다크 인터페이스 언어 —
거의 검정에 가까운 냉색 중립 배경, 헤어라인 보더, 채도를 극도로 아낀 UI, 그리고
**색은 오직 데이터에만 준다.**

지켜야 할 것

- 색 채널은 희소 자원이다. UI 크롬은 무채색으로 두고, 파란 계열은 데이터 인코딩에만 쓴다.
- 표면은 배경과 1~2단계 차이의 얕은 위계로만 나눈다. 그림자를 쌓지 않는다.
- 보더는 항상 1px 헤어라인. 라운드는 6–10px 사이 한 스케일.
- 예산이 걸린 의사결정에 쓰이는 도구다. **정밀해 보이는 것보다 정직해 보이는 것이 우선.**
  추정치는 추정치처럼 보여야 한다.

피해야 할 것

- 그라디언트 히어로, 글래스모피즘, 네온 액센트 한 방.
- 무지개 색 지도. 순차 데이터에 다색 램프.
- 아이콘 대신 이모지.
- 모든 카드에 액센트 레일 붙이기.

---

## 2. 색

### 2.1 중립색 · 표면

순수 회색이 아니라 **청록 쪽으로 살짝 기울인** 중립색. 지도의 파란 램프와 같은 계열에 앉아
화면 전체가 한 덩어리로 읽힌다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--ground` | `#08090A` | 페이지 바탕 |
| `--surface` | `#0E1113` | 패널·카드·차트 표면 |
| `--surface-2` | `#14181B` | 테이블 헤더, 인풋, 코드 |
| `--surface-3` | `#1A1F23` | 호버, 선택 상태 |
| `--border` | `#21262B` | 기본 헤어라인 |
| `--border-strong` | `#2E353B` | 강조 구분선, 축선 |
| `--ink` | `#F1F4F5` | 본문·수치 (대비 17.9:1) |
| `--ink-2` | `#99A2AA` | 보조 텍스트 (7.3:1) |
| `--ink-3` | `#6A737B` | 캡션·축 라벨 (4.0:1) |

`--surface`(`#0E1113`)가 차트 표면이다. 아래 모든 대비 수치는 이 값 기준.

### 2.2 순차 램프 — 갭 강도

단일 색상(파랑), 어두움 → 밝음, 7분위. 어두운 배경에서는 **어두운 쪽이 배경으로 물러나는 것**이
"0에 가까움"의 자연스러운 표현이다.

| 분위 | 값 | 표면 대비 |
|---|---|---|
| 1 (갭 최저) | `#1c5cab` | 2.87 |
| 2 | `#2a78d6` | 4.31 |
| 3 | `#3987e5` | 5.23 |
| 4 | `#5598e7` | 6.38 |
| 5 | `#6da7ec` | 7.61 |
| 6 | `#9ec5f4` | 10.65 |
| 7 (갭 최고) | `#cde2fb` | 14.39 |

규칙

- 77개 주가 전부 보여야 하므로 **가장 어두운 단계도 표면 대비 2:1을 넘긴다** (2.87).
  더 어두운 램프 스텝(`#184f95` 이하)은 코로플레스에 쓰지 않는다.
- 모든 폴리곤에 `rgba(255,255,255,0.10)` 1px 스트로크. 색이 비슷해도 경계가 보인다.
- 데이터 없음 = `#21262B` (보더색). 뒤로 물러나되 구멍처럼 보이지 않는다.
- 선택된 주는 색을 바꾸지 않고 `#F1F4F5` 2px 스트로크로 표시한다.
  **선택 상태가 값 인코딩을 덮어쓰면 안 된다.**

### 2.3 발산형 — 예대율

전국 평균이라는 자연스러운 중간값이 있는 지표에만 쓴다.

| 극 | 값 |
|---|---|
| 낮음 (예금 대비 여신 부족) | `#3987e5` → `#1c5cab` |
| 중간 (전국 평균) | `#2E353B` (무채색) |
| 높음 | `#e66767` → `#c04a4a` |

양쪽 팔의 단계 수를 같게 한다. 중간값에 색상을 주지 않는다.

> 지도는 **한 번에 한 레이어**만 보여준다. 순차 컨텍스트가 화면에 둘 이상 겹치는 상황을
> 만들지 않는다 — 그래서 두 번째 순차 색상이 필요 없다.

### 2.4 범주형 — 3슬롯까지만

| 슬롯 | 값 |
|---|---|
| 1 | `#3987e5` |
| 2 | `#d95926` |
| 3 | `#199e70` |

산점도·코로플레스처럼 **모든 쌍이 동시에 보이는** 형태에서 검증된 조합이다.

- 최악 쌍 색각이상 분리도 ΔE **9.4** (deutan, 목표 ≥8)
- 최악 쌍 정상시 분리도 ΔE **20.9** (하한 15)
- 세 색 모두 표면 대비 ≥3:1

4번째 계열이 필요하면 **색을 늘리지 않는다.** "기타"로 묶거나 차트를 분할한다.

### 2.5 상태색 — 고정, 테마 무관

| 역할 | 값 |
|---|---|
| good | `#0ca30c` |
| warning | `#fab219` |
| serious | `#ec835a` |
| critical | `#e66767` |

계열색으로 절대 재사용하지 않는다. 항상 아이콘·라벨과 함께 쓰고 **색 단독으로 의미를 지지 않는다.**

신뢰등급 배지도 이 계열을 쓴다 — 실측 `good`, 가공 `warning`, 추정 `serious`, 없음 `critical`.

---

## 3. 타이포그래피

| 역할 | 서체 | 설정 |
|---|---|---|
| 제목·본문 | **IBM Plex Sans KR** 300–700 | 라틴·한글이 같은 골격을 공유해 혼용 시 흔들리지 않는다 |
| 수치·라벨·축·코드 | **IBM Plex Mono** 400/500 | `font-variant-numeric: tabular-nums` |

```html
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=IBM+Plex+Sans+KR:wght@300;400;500;600;700&display=swap">
```

폴백 스택: `"IBM Plex Sans KR", system-ui, -apple-system, "Segoe UI", sans-serif`

### 스케일

| 역할 | 크기 | 굵기 | 트래킹 |
|---|---|---|---|
| H1 | `clamp(34px, 6vw, 52px)` | 600 | `-0.035em` |
| H2 | `clamp(21px, 3vw, 26px)` | 600 | `-0.028em` |
| H3 | 16.5px | 600 | `-0.018em` |
| 본문 | 15.5px / 1.72 | 400 | `-0.003em` |
| 테이블 | 13.5px / 1.55 | 400 | 0 |
| 축·캡션 (mono) | 10.5–11px | 400 | `+0.03em` |
| 대문자 라벨 (mono) | 10.5–11px | 500 | `+0.10em` |
| 히어로 수치 | 26–34px | 600 | `-0.035em` |

규칙

- 큰 크기일수록 더 조인다. 작은 mono 라벨은 오히려 벌린다.
- 본문 폭 65–70ch. 한글은 `word-break: keep-all`, 제목은 `text-wrap: balance`.
- **텍스트는 텍스트 토큰을 입는다.** 수치·범례 라벨에 계열색을 칠하지 않는다 —
  옆의 색 마크가 정체성을 나른다.

---

## 4. 레이아웃

- 데스크톱 우선. 분석 도구이므로 지도·사분면·테이블이 동시에 보여야 한다.
- 작업 영역 3분할: **지도(주) / 사분면·랭킹(부) / 상세 패널(슬라이드 인)**.
- 상단 고정 컨트롤 바 — 레이어 선택, 기준시점 배지, 공유. 스크롤과 무관하게 항상 접근 가능.
- 형제 그룹은 flex/grid + `gap`으로 배치한다. 요소별 margin으로 간격을 만들지 않는다.
- 넓은 콘텐츠(테이블·수식)는 **자기 컨테이너에** `overflow-x: auto`. 페이지 본문은 절대 가로 스크롤하지 않는다.
- 라운드 스케일: 5px(칩·배지) / 7–8px(인풋·작은 카드) / 10px(패널) 셋만 쓴다.
- 모바일(<768px): 지도 → 랭킹 → 상세 순으로 스택. 가중치 패널은 바텀시트.

---

## 5. 컴포넌트

**패널** — `--surface` 배경, `--border` 1px, 10px 라운드, 패딩 22/24px.
제목은 mono 11px 대문자 `var(--track-label)` `--ink-3`.

> **자간이 `+0.11em`이 아닌 이유.** 0.11em은 대문자 라틴 소제목의 관용값이다.
> 이 화면의 소제목은 전부 한글이고, 한글에 그 값을 주면 글자마다 1.2px씩 벌어져
> `갭  점수`처럼 읽힌다. 그래서 `--track-label: 0.02em`으로 낮췄다.
> 값은 `tokens.css` 한 곳에 있고 8개 소제목이 그것을 참조한다 — 여기서만 조정한다.
> `text-transform: uppercase`는 한글에 무해하고 섞인 라틴에만 걸리므로 유지한다.

**칩·배지** — 999px 라운드, `--surface-2` 배경, `--border` 1px, mono 11px, 패딩 4/11px.

**테이블** — 헤더 `--surface-2` + sticky. 셀 패딩 11/15px, 행 구분선 `--border`.
호버 시 `rgba(255,255,255,.017)`. 수치 열은 우측 정렬 + mono + `tabular-nums`.

**슬라이더** — 트랙 `--surface-3` 4px, 채워진 구간 `--accent`, 썸 12px 원 `--ink` + `--ground` 2px 링.
값은 항상 mono로 옆에 표기한다.

**툴팁** — `--surface-2` 배경, `--border-strong` 1px, 8px 라운드, 12.5px.
주 이름(sans 600) + 지표 행(mono, 라벨 `--ink-3` / 값 `--ink`) + 신뢰도 배지.
마크보다 큰 히트 타깃을 준다.

**범례** — 계열 2개 이상이면 항상 존재한다. 4개 이하면 직접 라벨도 같이 붙인다.
순차 램프는 가로 띠 + 양끝 값 + 중앙 눈금.

---

## 6. 모션

- 트랜지션 120–180ms, `cubic-bezier(0.2, 0, 0, 1)`.
- 지도 색 전환(레이어 변경)은 200ms 크로스페이드. 지오메트리는 움직이지 않는다.
- 상세 패널 슬라이드 인 180ms.
- 로드 시 순차 등장 애니메이션 없음. 도구는 즉시 준비되어 있어야 한다.
- `prefers-reduced-motion: reduce`면 전부 0ms.

---

## 7. 접근성

- 모든 텍스트가 표면 대비 4.5:1 이상 (`--ink-3` 4.0은 캡션·비필수 라벨 한정).
- 지도의 각 주는 실제 DOM 노드다 — 탭 순회 가능, `aria-label`에 주 이름과 갭 점수.
  이것이 MapLibre 대신 D3+SVG를 고른 이유 중 하나다.
- 포커스 링: `2px solid var(--accent)`, `outline-offset: 3px`. 절대 제거하지 않는다.
- 색만으로 의미를 전달하지 않는다 — 아키타입은 색 + 라벨 + 위치, 신뢰도는 색 + 텍스트.
- 모든 차트에 테이블 뷰 대안이 존재한다 (랭킹 테이블이 그 역할).
- `forced-colors` 모드에서 지도 폴리곤은 패턴 채움으로 대체한다.

---

## 8. 콘텐츠 톤

- 사용자 쪽 언어로 쓴다. "ADM1 코드"가 아니라 "주".
- 버튼은 일어날 일을 그대로 말한다. "적용"이 아니라 "가중치 반영".
- 에러는 사과하지 않고 무엇이 잘못됐고 어떻게 고치는지 말한다.
  "데이터를 불러올 수 없습니다" (X) → "2025-06 데이터가 아직 없습니다. 최신은 2025-05입니다." (O)
- 수치 옆에는 언제나 기준시점이 따라간다.
