# CLAUDE.md

이 리포에서 작업할 때 지켜야 할 것. 기획 배경은 `README.md`,
지수 정의는 `METHODOLOGY.md`, 디자인은 `DESIGN.md`, 할 일은 `docs/BACKLOG.md`.

## 이 프로젝트가 무엇인가

태국 77개 주의 금융 공급과 수요의 격차를 지수화해 지도로 보여주는 **정적 웹 도구**.
에이전트·키오스크·지점 확장 우선순위를 정하는 데 쓴다.
런타임 백엔드가 없다 — ETL이 CI에서 정적 JSON을 굽고, 사이트는 그걸 fetch한다.

## 절대 하지 말 것

- **결측을 0으로 채우지 마라.** 주 단위 원본이 있는 지표에 결측이 나오면 실패시킨다.
  `validate.py`의 게이트를 우회하지 마라 — 그게 파서가 조용히 깨지는 걸 잡는 유일한 장치다.
- **추정치에 지도의 색 채널을 주지 마라.** 디지털 준비도는 주 단위 원본이 없다
  (권역값 하향 추정). 사분면 축과 필터에서만 쓰고, 항상 `추정` 배지를 붙인다.
- **Findex를 주별 레이어로 승격하지 마라.** 표본 n=1,000, 지역 식별자 없음.
  전국 벤치마크 패널 전용이다. `findex.json`의 `not_by_province: true`와
  `FindexPanel`의 "주별 아님" 배지가 그 장치다 — 둘 다 props로 끌 수 없게 되어 있다.
- **Findex 마이크로데이터를 다운로드·커밋하지 마라.** 라이선스상 재배포 불가.
  지표 API의 집계값만 쓴다.
- **무지개 색 램프를 쓰지 마라.** 순차는 단일 색상, 발산은 두 색 + 무채색 중간값.
- **범주형 색을 4개 이상 쓰지 마라.** 3슬롯까지만 검증되어 있다. 4번째가 필요하면
  "기타"로 묶거나 차트를 분할한다.
- **`data/raw/`, `public/data/`를 커밋하지 마라.** gitignore에 있다.
- **내부 데이터(TrueMoney 에이전트 등)를 이 리포에 넣지 마라.** 공개 데이터만 유지한다.
- **자격증명을 커밋하지 마라.** 이 저장소는 **공개**다. v1의 모든 소스는 인증이 없다.
  인증이 필요한 소스(BOT API 포털 등)는 v1 경로가 아니다.
- **푸터의 데이터 귀속 표기를 제거하지 마라.** OpenStreetMap(ODbL)과 행정경계(CC BY)
  라이선스상 의무다. `src/config/attribution.ts`.
- **`ParserDriftError`를 try/except로 삼키지 마라.** 그 예외가 존재하는 이유가
  "틀린 숫자를 만드는 것보다 멈추는 게 낫다"이다. 지문이 안 맞으면 지문을 고쳐라.
- **디지털 준비도를 값으로 채우지 마라.** v1에서는 입력 두 개(NSO 이용률·주별 도시화율)가
  모두 없어서 `null`이다. `archetype`도 그래서 `null`이다 — 절반을 찍지 않는다.
  근거와 되살릴 조건은 `DATA_SOURCES.md`의 "디지털 축" 절.
- **캐시 폴백을 아무 소스에나 붙이지 마라.** `nesdc_gpp`에만 있고, 근거는
  공표 주기가 1년이라 캐시본과 라이브본이 같은 파일이라는 것뿐이다.
  BOT(월간)에 같은 것을 붙이면 낡은 달의 수치를 새 달인 척 배포하게 된다.
  그리고 **파싱 실패는 캐시로 덮지 않는다** — 그건 장애가 아니라 구조 변경 신호다.
- **보조 소스가 빠진 달을 그냥 통과시키지 마라.** 지표가 결측이 되면 `_weighted()`가
  가중치에서 빼고 재정규화한다 — 값이 틀린 게 아니라 **모든 주의 축 점수가 이동한다**
  (Overpass 한 번 실패에 공급 평균 2.7pt·최대 7.9pt·순위 7계단). 눈에 안 띄는 종류라
  `validate.py`의 '사라진 지표' 게이트가 세운다. `indicator_columns`를 줄이지 마라.
- **보조 소스 실패를 필수 소스처럼 다루지 마라.** `admin_ref`·`bot_province`·`nesdc_gpp`는
  실패하면 빌드가 죽는다. `nso_ict`·`osm_atm`·`findex`는 결측으로 두고 계속 간다.
  `build.py`의 `SOURCES` 목록이 그 등급을 정의한다.

## 공개 저장소다

`README.md`의 "공개 저장소" 절이 규칙이다. 요약하면: 공개 출처 데이터만,
자격증명 없이, 귀속 표기를 달고, 내부 데이터는 별도 프라이빗 저장소로.

## BOT 스크래핑이 유일한 경로다

BOT API 포털 등록을 미뤘다. `bot_province.py`가 v1의 단일 데이터 경로이므로
방어 장치를 약화시키지 마라.

- `check_schema()`의 지문이 안 맞으면 **파싱을 진행하지 않는다.** 이건 버그가 아니라 기능이다.
- BOT이 표를 바꿔서 카나리가 터지면, 스냅샷(`data/raw/`)을 보고
  `COLUMNS` / `EXPECT_HEADER_TOKENS` / `EXPECT_COLUMNS` / `EXPECT_PROVINCE_ROWS`를
  **의도적으로** 갱신한다. 검사를 느슨하게 만들어 통과시키지 마라.
- 열 정렬 검사는 네 겹이다 — 열 수(15) · 헤더 토큰 13개 · 주 행 수(77) ·
  **공표 예대율과 `credits/deposits` 교차 검증**. 마지막 것이
  "구조는 그대로인데 값만 밀린" 경우를 잡는 유일한 그물이니 특히 지우지 마라.
- 표의 0열은 일련번호이고 주 이름은 1열이다. 금액 단위는 백만 바트이며
  `bot_province.py`가 한 번만 바트로 환산한다 — 다른 곳에서 또 환산하지 마라.
- 회귀는 `tests/test_bot_parser.py`가 지킨다. 픽스처는 실제 응답에서 표만 남긴 것이다.
  파서를 고치면 여기에 케이스를 추가한다.
- 파서가 깨진 동안 배포되는 것은 낡은 데이터가 아니라 **직전 데이터 그대로**다.
  갱신 PR이 안 열릴 뿐 사이트는 멀쩡하다. 급하게 우회할 이유가 없다.

## 조인 규칙

**TIS-1099 2자리 코드가 마스터 키다.** BOT는 영문 주명 문자열,
geoBoundaries는 shapeID, DOPA는 숫자 코드를 쓴다.
`thailand-canonical-admin-names`의 크로스워크(`name_alternates_en` + `overrides.csv`)가
그 넷을 잇는 유일한 검증된 다리다.

BOT의 영문 표기는 표준과 자주 다르다 (`Chiengmai`, `Prachuapkhirikhun`, `Phrea`).
매칭 실패는 별칭 테이블에 추가해서 고치고, 이름을 하드코딩으로 치환하지 마라.
로컬 별칭은 `data/reference/province_name_aliases.csv` 한 곳에만 둔다 —
상류 크로스워크가 흡수한 별칭은 `admin_ref.load()`가 경고하니 그때 지운다.

**NSO 5권역과 크로스워크 6권역을 섞지 마라.** 크로스워크의 `region`은 East·West를 분리한
6권역이고, NSO·NESDC·BOT은 5권역(Bangkok/Central/North/Northeast/South)을 쓴다.
하향 추정과 권역 집계에는 BOT 표의 소계 행에서 읽은 `region_nso`를 쓴다.

**TIS-1099는 식별자다.** 파이프라인 내부에서는 int로 조인하되 산출물(JSON·TopoJSON)에는
2자리 문자열로 내보낸다. 타입이 갈라지면 전월 대비 병합이 조용히 비어
급변 감지가 꺼진다 (`validate.py`가 이제 그것도 잡는다).

## 두 곳에 같은 로직이 있다

`etl/transform/figi.py` (Python)와 `src/lib/score.ts` (TypeScript)는
같은 지수 산식을 구현한다. 가중치 슬라이더가 브라우저에서 재계산해야 하기 때문.

**한쪽을 고치면 반드시 다른 쪽도 고친다.** `tests/test_parity.py`가 이를 지킨다.
산식의 단일 출처는 `METHODOLOGY.md`다 — 코드와 문서가 어긋나면 코드가 틀린 것으로 본다.

**중간값을 반올림하지 마라.** 반올림은 표시의 문제다. 계산 중간에 끼어들면
`gap_raw`가 0.01 어긋나고 그 오차가 `priority`에서 `log10(인구)`만큼 증폭된다.
양쪽 모두 전체 정밀도로 계산하고 **출력 직전에 한 번만** 반올림한다.
(실제로 이것 때문에 두 구현이 갈라졌고 패리티 테스트가 잡았다 — 2026-08-31.)

## 화면은 문서가 아니라 meta.json을 믿는다

`Methodology` 패널이 `METHODOLOGY.md`를 렌더하지 않고 `meta.json`을 렌더하는 이유다.
문서는 '이렇게 만들 계획'이고 meta.json은 '이번 빌드가 실제로 만든 것'이다.
지금이 정확히 둘이 어긋난 상황이다 — 디지털 축은 계획 `estimated`, 실제 `missing`.

- `build.py`의 `_index_block()`이 config의 계획값을 실제 결과로 덮고
  계획값은 `confidence_planned`로 남긴다. 이 덮어쓰기를 지우지 마라.
- 확보 실패한 소스는 `degraded_sources`에 사유와 함께 남고 화면에 그대로 나온다.
  숨기지 마라 — 빈 차트를 보고 "미구현"으로 오해하는 것이 더 나쁘다.

## 스타일

- 색·타이포·간격은 `src/styles/tokens.css`의 토큰으로만 쓴다. 리터럴 hex 금지.
- 여러 컴포넌트가 함께 쓰는 클래스(`.panel` `.chip` `.sr-only`)는 `tokens.css`에 둔다.
  컴포넌트 CSS에 두면 그 파일을 import하지 않는 화면에서 조용히 스타일이 빠진다.
- 라운드는 `--r-sm` / `--r-md` / `--r-lg` 셋만.
- 형제 그룹은 flex/grid + `gap`. 요소별 margin으로 간격을 만들지 않는다.
- 넓은 콘텐츠는 자기 컨테이너에 `overflow-x: auto`. 페이지 본문은 가로 스크롤하지 않는다.
- 수치는 mono + `tabular-nums`. 텍스트에 계열색을 칠하지 않는다.

## 접근성 (타협 대상 아님)

지도를 D3 + SVG로 만든 이유 중 하나가 이것이다 — 각 주가 실제 DOM 노드다.

- 모든 주가 탭으로 순회 가능하고 `aria-label`에 이름과 갭 점수가 들어간다.
- 포커스 링을 제거하지 않는다.
- 색만으로 의미를 전달하지 않는다. 아키타입은 색 + 라벨 + 위치.
- 모든 차트에 테이블 뷰 대안이 있다 (랭킹 테이블).

## 커밋

- ETL 산출물 변경은 `data:` 접두어. 코드와 데이터를 같은 커밋에 섞지 않는다.
- 데이터 갱신은 자동 머지하지 않는다. PR을 열고 사람이 diff를 본다.
