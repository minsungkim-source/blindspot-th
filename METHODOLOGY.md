# METHODOLOGY — FIGI (Financial Inclusion Gap Index)

이 문서는 화면의 모든 숫자가 어떻게 나오는지 재현 가능한 수준으로 적는다.
사이트의 "방법론" 섹션은 이 파일을 그대로 렌더링한다.

---

## 0. 한 문장 요약

> **갭 = 수요 − 공급.** "공급이 적다"가 아니라 "받쳐줄 수요가 있는데도 공급이 적다"를 찾는다.

---

## 1. 분석 단위

- **ADM1 · 태국 77개 주** (방콕 포함).
- 조인 키는 **TIS-1099 2자리 코드**. BOT는 영문 주명 문자열, geoBoundaries는 shapeID,
  DOPA는 숫자 코드를 쓰므로, `thailand-canonical-admin-names`의 크로스워크를
  마스터로 두고 모든 소스를 여기에 붙인다.
- 부칸(Bueng Kan, 2011년 Nong Khai에서 분리)의 과거 시계열은
  `historical_mappings.csv`에 따라 처리한다. 2011년 이전 데이터를 비교할 때만 문제가 된다.

---

## 2. 정규화

모든 지표는 **77개 주 내 백분위 순위**로 변환한다.

```
pct(x_p) = 100 × rank(x_p) / (N − 1),  N = 77,  동점은 평균 순위
```

### 왜 z-score가 아닌가

방콕이 거의 모든 지표에서 극단 이상치다. 예금 잔액은 2위 주의 수십 배다.
z-score를 쓰면 방콕이 +7σ 근처에 서고 나머지 76개 주가 −0.3σ 부근에 한 덩어리로 눌려
지도에서 서로 구분되지 않는다.

백분위는 그 왜곡이 없고, 비전문 이해관계자에게 **"77개 중 몇 위"** 로 설명하기도 쉽다.
대가는 절대 크기 정보의 손실인데, 그건 툴팁의 원값과 규모 가중 우선순위가 보완한다.

로그를 먼저 취하는 지표(`log(인구)`, `log(1인당 예금)`)는 백분위 변환 전에 적용한다.
백분위는 단조변환에 불변이므로 순위 자체는 바뀌지 않지만, 결측 대체와
Phase 2의 연속형 시각화에서 의미가 생긴다.

### 결측 처리

- **주 단위 원본이 있는 지표에 결측이 나오면 대체하지 않고 실패시킨다.** 77개가 전부 있어야 한다.
- 주 단위 원본이 없는 지표(디지털 축)는 §5의 하향 추정 절차를 쓰고 `estimated`로 표시한다.
- OSM ATM처럼 0이 "정말 0"인지 "매핑이 안 된 것"인지 구분되지 않는 지표는
  가중치를 낮게 두고(0.10) 신뢰등급 `C`로 표시한다.
- **보조 지표가 통째로 결측이면 가중치에서 빼고 남은 것끼리 재정규화한다.** 0으로 채우지 않는다.
  ATM을 세지 못한 주는 ATM이 없는 주와 다르다 — 0으로 채우면 공급 점수가 부당하게 낮아지고,
  그 주가 우선순위 상위로 올라온다.

  ```
  Axis(p) = Σ w_i · pct_i(p)  /  Σ w_i        (pct_i(p)가 존재하는 i에 대해서만)
  ```

---

## 3. 공급 축 (Supply)

전부 BOT 실데이터. 기본 가중치는 합이 1.0.

| # | 지표 | 계산 | w |
|---|---|---|---|
| S1 | 지점 밀도 | `지점수 / (인구/100,000)` | 0.30 |
| S2 | 지리적 접근성 | `지점수 / (면적km² / 1,000)` | 0.20 |
| S3 | 예금 침투 | `log(총예금 / 인구)` | 0.20 |
| S4 | 신용 침투 | `log(총여신 / 인구)` | 0.20 |
| S5 | ATM 밀도 | `OSM ATM수 / (인구/100,000)` | 0.10 |

```
Supply(p) = Σ wᵢ · pct(Sᵢ(p))     → 0..100
```

**S1과 S2를 나눠 쓰는 이유** — 같은 인구라도 면적이 넓으면 주민 한 사람의 이동 거리가 길다.
Mae Hong Son(12,681km², 인구 24만)과 Nonthaburi(622km², 인구 130만)를
"인구당 지점 수" 하나로 비교하면 접근성의 절반을 놓친다.

---

## 4. 수요 축 (Demand)

공급이 **가야 마땅한 정도**. 인구가 많고, 소득은 낮고, 넓게 흩어져 살고,
현금경제 비중이 높고, 예금 대비 여신이 덜 나갈수록 높다.

| # | 지표 | 계산 | w |
|---|---|---|---|
| D1 | 인구 규모 | `log(인구)` | 0.30 |
| D2 | 소득 하방 | `100 − pct(GPP per capita)` | 0.25 |
| D3 | 분산 거주 | `100 − pct(인구/면적)` | 0.20 |
| D4 | 현금경제 비중 | GPP 중 농림어업 비중 | 0.15 |
| D5 | 신용 갈증 | `100 − pct(총여신/총예금)` | 0.10 |

```
Demand(p) = Σ vⱼ · pct(Dⱼ(p))     → 0..100
```

**D5(예대율 역수)에 대한 주의** — 낮은 예대율은 두 가지로 읽힌다:
(a) 신용 공급이 부족하다 = 기회, (b) 신용 수요·상환능력이 약하다 = 리스크.
이 지표만으로는 구분되지 않으므로 가중치를 낮게(0.10) 두고,
상세 패널에서 예대율을 **발산형 지도 레이어로 따로** 보여준다.

---

## 5. 디지털 준비도 (Digital Readiness) — 추정치

**주 단위 원본이 존재하지 않는다.** NSO ICT 가구조사는 5개 권역 × 시가지/비시가지까지만
공표하고, NBTC 오픈데이터도 전국 단위다.

```
R(p) = u(p) · rate[region(p), urban] + (1 − u(p)) · rate[region(p), rural]
```

- `u(p)` = 주 p의 도시화율 (NSO 인구센서스 municipal area 비율)
- `rate[r, urban|rural]` = NSO ICT 가구조사의 권역 × 지역구분 인터넷 이용률
- `region(p)` = 주 p가 속한 5개 권역 (Bangkok / Central / Northeast / North / South)

### 이 추정이 놓치는 것

같은 권역·같은 도시화율의 두 주는 **정의상 같은 값을 받는다.** 실제로는 다를 수 있다.
따라서:

1. 디지털 축은 **지도의 색 채널을 차지하지 않는다.** 사분면의 가로축과 필터에서만 쓴다.
2. 모든 표시에 `추정` 배지가 붙는다.
3. 사분면의 좌우 분류는 **경계 근처에서 신뢰할 수 없다.** 중앙값 ±5pt 구간의 주는
   회색 처리하고 "경계"로 라벨한다.
4. Phase 2에서 내부 앱 MAU/인구 비율로 검증·보정한다.

### 입력이 없으면 추정하지 않는다

이 식은 입력 두 개를 요구한다 — 권역별 이용률 `rate[r, ·]`와 주별 도시화율 `u(p)`.
**둘 중 하나라도 없으면 `R(p) = null`이다.** 대리 지표로 메우거나 전국값으로 채우지 않는다.
없는 축을 있는 척하면 사분면이 근거 없는 좌우 분류를 만들어낸다.

> **v1 현재 상태: `R(p) = null` (전 주).** NSO 카탈로그 API가 차단되어 `rate`를 얻지 못하고,
> 주별 도시화율을 주는 공개 소스도 없다. 산출물에 `digital_confidence: "missing"`이 실리고
> `meta.json`의 `degraded_sources`에 사유가 남는다. 상세는 `DATA_SOURCES.md`의 "디지털 축" 절.

---

## 6. 갭과 우선순위

```
GAP_raw(p)  = Demand(p) − Supply(p)          → −100 .. 100
GAP(p)      = (GAP_raw(p) + 100) / 2          → 0 .. 100   (화면 표시용)

Priority(p) = GAP_raw(p) × log₁₀(인구(p))
              정규화: 최대값으로 나눠 0..100
```

**Priority가 따로 필요한 이유** — 갭 점수만 보면 인구 20만 명의 소규모 주가 상위에 온다.
갭이 크더라도 절대 인구가 작으면 에이전트 한 곳의 단위경제가 안 나온다.
`log₁₀(인구)`로 규모를 곱해 "고칠 가치가 있는 갭"을 앞으로 보낸다.
지도는 GAP, 랭킹 테이블 기본 정렬은 Priority.

---

## 7. 액션 아키타입

`GAP`과 `R`(디지털 준비도)의 **중앙값**을 기준으로 4분면.

| GAP | Digital | 아키타입 | 개입 수단 |
|---|---|---|---|
| ≥ 중앙값 | ≥ 중앙값 | **디지털 우선** | 원격 KYC, 앱 획득, 디지털 대출 파일럿 |
| ≥ 중앙값 | < 중앙값 | **에이전트 · 키오스크** | 에이전트 리크루팅, 키오스크, 캐시인/아웃 유동성 |
| < 중앙값 | ≥ 중앙값 | **유지 · 크로스셀** | 상품 침투, 저예대율이면 대출 푸시 |
| < 중앙값 | < 중앙값 | **관망** | 분기 재평가, 인접 주 확장의 부수 효과 |

평균이 아니라 **중앙값**을 쓴다. 방콕 때문에 평균이 끌려간다.

**`R(p)`가 없으면 아키타입도 없다.** 디지털 축이 결측인 주는 `archetype = null`이고,
전 주가 결측이면 사분면과 아키타입 필터가 통째로 비활성화된다.
가로축이 없는 상태에서 좌우를 가르는 것은 동전 던지기이고, 그 결과가 "이 주에는
에이전트를 깐다"는 판단으로 이어진다. 절반을 찍느니 분류하지 않는다.

경계 처리는 그대로다 — `R(p)`가 있어도 중앙값 ±5pt 안이면 `borderline`으로 표시하고
좌우 분류를 신뢰하지 않는다.

---

## 8. 가중치 조정

화면의 슬라이더로 `w₁..w₅`, `v₁..v₅`를 바꿀 수 있고, 상태는 URL 쿼리에 인코딩된다.
가중치는 항상 합 1.0으로 재정규화된다.

프리셋:

| 프리셋 | 성격 |
|---|---|
| 균형 | 위 표의 기본값 |
| 규모 우선 | D1 0.50까지 — 큰 시장부터 |
| 원격지 우선 | S2 0.35, D3 0.35 — 이동거리 중심 |
| 신용 갈증 | S4 0.35, D5 0.30 — 대출 상품 화이트스페이스 |

> **처음 열었을 때 보이는 순위가 사실상의 공식 견해가 된다.**
> 기본 가중치는 영업·리스크·전략이 함께 확정해야 한다.

---

## 9. 알려진 한계

1. **지점 ≠ 접근성.** 은행 에이전트, 우체국, 협동조합, 모바일 채널이 공급 축에 빠져 있다.
   이미 에이전트로 커버된 주가 과대 갭으로 표시될 수 있다.
   Phase 2의 내부 에이전트 레이어가 이 편향의 직접 교정이다.
2. **BAAC(농협은행) 취급.** BOT 상업은행 통계는 특수목적은행을 포함하지 않는다.
   농촌 지역의 실제 금융 접점은 이 지수가 보는 것보다 많다.
3. **시점 불일치.** BOT 월간, GPP 연간, NSO ICT 연간, Findex 2024 조사.
   화면에 각각의 기준시점을 표기하되, 지수 자체는 "가장 최근 확보 가능한 값"으로 계산한다.
4. **주 내부 편차.** 한 주 안에서도 도청 소재지와 산간 군의 접근성은 크게 다르다.
   ADM2 드릴다운이 이를 부분적으로 해소한다(306개 군 실측 가능).
5. **Findex는 주별이 아니다.** 표본 n=1,000, 하위지역 식별자 없음.
   전국 벤치마크로만 쓰며 지도 레이어로 승격하지 않는다.
6. **OSM ATM 커버리지 편차.** 도시가 농촌보다 잘 매핑되어 있어
   갭을 **과소평가**하는 방향으로 편향된다. 가중치 0.10, 신뢰등급 C.

---

## 10. 재현

```bash
cd etl && python build.py
```

`data/processed/meta.json`에 각 소스의 URL, 수집 시각, 기준시점, 파일 해시가 기록된다.
같은 입력이면 같은 출력이 나온다 (ATM은 Overpass 실시간 조회라 예외 — 스냅샷을 `data/raw`에 남긴다).
